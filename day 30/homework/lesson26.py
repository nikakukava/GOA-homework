#1. Given a list of strings, filter out the strings that start with a specific prefix using the startswith() method.

list = ["list", "string","lake"]
for i in list:
    print(i.startswith("l"))
#2. Check if user input starts with a specified character or substring using the startswith() method for validation.

string = "sigma sigma on the wall"
print(string.startswith("sigma"))