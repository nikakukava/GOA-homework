#1. Create a list and use the len() function to count the number of elements in it.
list = ["asd",34,432.5,True]
print(len(list))

#2. Take user input as a string and then use the len() function to determine its length.
string = str(input("please enter a string : "))
print(len(string))